const execute=document.querySelector('#execute');const replay=document.querySelector('#replay');const count=document.querySelector('#count');function action(btn,label){const old=btn.textContent;btn.textContent=label;btn.disabled=true;if(btn===execute)count.textContent=(18492+1).toLocaleString();setTimeout(()=>{btn.textContent=old;btn.disabled=false},1000)}execute?.addEventListener('click',()=>action(execute,'Queued'));replay?.addEventListener('click',()=>action(replay,'Replay ready'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>{document.querySelectorAll('nav a').forEach(x=>x.classList.remove('active'));a.classList.add('active')}));